#Language used
language: python

#python version used
python:
  - "3.5.2"

#Environment variable for the build
env: 
- PRIV=fast-food-fast

#packages to be installed
install:
  - pip install -r requirements.txt

#run tests with
script:
  - nosetests  --with-coverage --cover-package=app/a_p_i/v1/views

# command to run test coverage
after_success:
  - coveralls